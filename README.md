# -_evaluacion_de_derivadas_infinitas_- :.
📘 Evaluación de Derivadas Infinitas:

<img width="1024" height="1024" alt="image" src="https://github.com/user-attachments/assets/dfd6b196-d028-43e8-ac45-b704f957d4ff" />  

```
Solución técnica completa: algoritmo + validación matemática + persistencia en Oracle 19c .

🧠 1. Enfoque del Problema:

Determinar si una derivada es infinita implica evaluar el siguiente límite:

```

<img width="267" height="70" alt="image" src="https://github.com/user-attachments/assets/37c970d9-2bff-4056-8a68-ffec00c4ce1c" />  

```

Esto ocurre típicamente cuando:

Existen asíntotas verticales
El denominador de la derivada tiende a 0
La función presenta crecimiento no acotado .

⚙️ 2. Algoritmo (Lógica):
🎯 Entrada
Funcion 
𝑓(𝑥)
Punto 
𝑎

🎯 Salida
"INFINITA"
"FINITA"
"INDETERMINADA"

🔁 Pseudocódigo (3 condicionales anidados) .
INICIO

leer funcion f(x)
leer punto a

calcular derivada f'(x)

evaluar limite L = lim x->a de f'(x)

SI (existe el limite) ENTONCES
    SI (|L| es muy grande o tiende a infinito) ENTONCES
        resultado = "INFINITA"
    SINO
        resultado = "FINITA"
    FIN SI
SINO
    SI (hay division por cero o discontinuidad) ENTONCES
        resultado = "INFINITA"
    SINO
        resultado = "INDETERMINADA"
    FIN SI
FIN SI

guardar resultado en BD

FIN

🗄️ 3. Modelo en Oracle 19c:
📌 Tabla
CREATE TABLE DERIVADAS (
    ID              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FUNCION         VARCHAR2(200),
    PUNTO           NUMBER,
    DERIVADA        VARCHAR2(200),
    RESULTADO       VARCHAR2(50),
    FECHA_REGISTRO  DATE DEFAULT SYSDATE
);

📌 Procedimiento Almacenado
CREATE OR REPLACE PROCEDURE INSERTAR_DERIVADA (
    P_FUNCION   IN VARCHAR2,
    P_PUNTO     IN NUMBER,
    P_DERIVADA  IN VARCHAR2,
    P_RESULTADO IN VARCHAR2
)
AS
BEGIN
    INSERT INTO DERIVADAS (FUNCION, PUNTO, DERIVADA, RESULTADO)
    VALUES (P_FUNCION, P_PUNTO, P_DERIVADA, P_RESULTADO);

    COMMIT;
END;
/

💻 4. Implementación en C# (.NET 8):
⚠️ Nota: Se realiza evaluación numérica (no simbólica completa) . 

📌 Lógica principal
using System;

class DerivadaEvaluator
{
    public static string Evaluar(double numerador, double denominador)
    {
        string resultado;

        if (!double.IsNaN(numerador) && !double.IsNaN(denominador))
        {
            if (Math.Abs(denominador) < 0.000001)
            {
                if (Math.Abs(numerador) > 0)
                {
                    resultado = "INFINITA";
                }
                else
                {
                    resultado = "INDETERMINADA";
                }
            }
            else
            {
                resultado = "FINITA";
            }
        }
        else
        {
            resultado = "INDETERMINADA";
        }

        return resultado;
    }
}

📌 Ejemplo de uso
double numerador = 5;
double denominador = 0.0000001;

string resultado = DerivadaEvaluator.Evaluar(numerador, denominador);

Console.WriteLine($"Resultado: {resultado}");

🔌 5. Inserción en Oracle (ODP.NET):
using Oracle.ManagedDataAccess.Client;

public static void Guardar(string funcion, double punto, string derivada, string resultado)
{
    string connectionString = "User Id=USER;Password=PASS;Data Source=localhost:1521/XEPDB1";

    using var conn = new OracleConnection(connectionString);
    conn.Open();

    using var cmd = new OracleCommand("INSERTAR_DERIVADA", conn);
    cmd.CommandType = System.Data.CommandType.StoredProcedure;

    cmd.Parameters.Add("P_FUNCION", funcion);
    cmd.Parameters.Add("P_PUNTO", punto);
    cmd.Parameters.Add("P_DERIVADA", derivada);
    cmd.Parameters.Add("P_RESULTADO", resultado);

    cmd.ExecuteNonQuery();
}

🧪 6. Datos de Prueba:

```

<img width="815" height="264" alt="image" src="https://github.com/user-attachments/assets/09b962ae-f745-4a53-89d5-2af81333ee65" />  

```

📊 7. Mejoras Posibles:

Para un sistema más robusto:

Integrar librerías simbólicas:
Math.NET Symbolics

Evaluar límites laterales:

```

<img width="219" height="59" alt="image" src="https://github.com/user-attachments/assets/1a56f07b-d3eb-4866-9864-f2f32d892caa" />  

```  
  ​
Detectar asíntotas automáticamente
Implementar UI:
Consola
WPF / WinForms
Escalar a arquitectura MVC .

✅ Conclusion:
El algoritmo:

Implementa 3 niveles de decisión anidados.
Evalua condiciones matematicas reales.
Permite persistencia en Oracle 19c.
Es escalable a arquitecturas empresariales (MVC) / : . / .  
